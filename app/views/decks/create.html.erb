<div class="container">
	<div class="row">
		<div class="col-md-8">
			<div class="container card">
				DECK STATUS
			</div>
		</div>
		<div class="col-md-4">
			<div class="container card">
				<% if @list.count >= 1 %>
					<%= render "cards/list", list: @list %>
				<% else %>
					<div>
						Nenhum registro encontrado
					</div>
				<% end %>
			</div>
		</div>
	</div>
</div>